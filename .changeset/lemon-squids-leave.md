---
"aws_elastic_beanstalk_nextjs_cicd": patch
---

Added a PR Lifecycle Comments workflow that automatically posts a comment on every newly opened pull request, instructing contributors to use /deploy to spin up a staging environment and /destroy to tear it down.
