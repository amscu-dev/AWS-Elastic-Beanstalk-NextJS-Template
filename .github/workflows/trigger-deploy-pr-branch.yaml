name: Trigger Deploy PR Branch

run-name: "Trigger Deploy PR #${{ github.event.issue.number }} by @${{ github.event.comment.user.login }}"
on:
  issue_comment:
    types: [created]

jobs:
  trigger-pr-branch-deploy:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    if: github.event.issue.pull_request && github.event.issue.state == 'open' && github.event.comment.body == '/deploy'
    steps:
      - name: Get Pull Request Head Ref
        id: get-head-ref
        uses: actions/github-script@v9
        with:
          result-encoding: string
          script: |
            const response = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.issue.number
            });
            console.log(response.data)
            return response.data.head.ref
      - name: Trigger a Branch Deploy
        uses: actions/github-script@v9
        with:
          script: |
            const response = await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "deploy-ephemeral-env-aws.yaml",
              ref: "${{ steps.get-head-ref.outputs.result }}",
              inputs: {
                "aws-env": `app-pr-${context.issue.number}`,
                "github-env": "staging",
                "report-pr": `${context.issue.number}`
              }
            })
            console.log(response)
